import React from 'react';
import LOGO from "./assets/images/LOGO.png";

const header = ({title}) => {
    return (
        <header>
            <p>{title}</p>
            <source />
        </header>
    );
};

export default header;